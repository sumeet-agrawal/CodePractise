#include <iostream>
#include <unordered_set>
#include <vector>

using namespace std;
int main()
{

    /*int arr1[] = {1, 2, 3,5 ,7 ,8};
    int arr2[] = {3, 8, 9, 12, 15, 1};

    //output = 1, 3, 8;

    unordered_set<int> st;
    for(auto x: arr1)
        st.insert(x);
    vector<int> ans{};
    for(auto x: arr2)
        if(st.count(x))
            ans.push_back(x);

    for(auto &x: ans)
        cout<<x<<" ";
    cout<<"\n";*/
    return 0;
};